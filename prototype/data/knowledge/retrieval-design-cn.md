# 检索设计说明

在第一阶段，还不需要一上来就依赖完整向量数据库。更现实的路线，是先把本地 markdown 或 text 文档切成较小片段，再通过轻量标签和关键词匹配完成最小检索。

这种做法的价值不在于它已经是最终架构，而在于它足够本地优先、足够容易解释、也足够方便后来者理解整个链路。

一个合格的第一阶段检索层，至少要做到三件事：一是回答时保留 source 引用；二是在文档未变化时尽量复用已有索引；三是为后续更复杂的排序、重排、向量化升级留出清晰替换点。
